jwt:
  secret: thisismysuoercomplexcodewithsrbsecret

server:
  error:
    path: /api/v1/booking/error
    whitelabel:
      enabled: false

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/BookYourSlot
    username: root
    password: srb-secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
  sql:
    init:
      mode: never
      continue-on-error: false